#!/usr/bin/env bash
# bin/compile <build-dir> <cache-dir>

set -eo pipefail


indent() {
  sed -u 's/^/       /'
}

echo "-----> Fetching rsync"

curl -L -o rsync.tar.gz http://f.cl.ly/items/241p0R2f2T1z0L321J1W/rsync.tar.gz

set +e
OUT=$(tar -zxf rsync.tar.gz)
