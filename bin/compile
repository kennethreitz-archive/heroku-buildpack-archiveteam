#!/bin/sh

indent() {
  sed -u 's/^/       /'
}

echo "-----> Fetching rsync"

curl -L -o rsync.tar.gz https://gist.github.com/raw/1787910/dirsnap-flfH0E-snapshot.tar.gz

set -e
tar xvfz rsync.tar.gz
